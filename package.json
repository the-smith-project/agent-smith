{
  "name": "agent-smith",
  "version": "0.1.0",
  "description": "Open-source runtime prompt-injection defense for OpenClaw",
  "main": "dist/core/index.js",
  "types": "dist/core/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "check": "npm test && npm run build",
    "test:watch": "jest --watch",
    "scan": "node scripts/scan.js",
    "scan:dev": "tsx scripts/scan.ts",
    "scan:batch": "node scripts/scan-batch.js",
    "verify:claude": "node scripts/verify-claude.js",
    "verify:moltbook": "node scripts/verify-moltbook.js",
    "verify:moltbook-search": "node scripts/verify-moltbook-search.js",
    "verify": "node scripts/verify.js"
  },
  "keywords": ["openclaw", "prompt-injection", "security"],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "dotenv": "^16.4.5",
    "xxhash-wasm": "^1.0.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
